# Локальная разработка (создаётся автоматически после `npx convex dev`)
VITE_CONVEX_URL=https://your-deployment.convex.cloud
VITE_CONVEX_SITE_URL=https://your-deployment.convex.site

# Vercel — добавьте в Settings → Environment Variables:
# VITE_CONVEX_URL = URL prod-деплоя Convex (после `npx convex deploy`)
